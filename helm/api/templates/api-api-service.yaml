apiVersion: v1
kind: Service
metadata:
  name: api-api
  annotations:
    cloud.google.com/backend-config: '{"default": "api-health-check"}'
spec:
  selector:
    app: api-api
  ports:
    - port: 80
      targetPort: {{ .Values.apiApi.port }}
