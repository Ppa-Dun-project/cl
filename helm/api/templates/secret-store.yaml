apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: api-secret-store
spec:
  provider:
    gcpsm:
      projectID: v2-ppa-dun
      auth:
        workloadIdentity:
          clusterLocation: us-east1
          clusterName: ppa-dun-cluster
          serviceAccountRef:
            name: api-secrets-ksa
