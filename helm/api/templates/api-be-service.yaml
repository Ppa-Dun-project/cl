apiVersion: v1
kind: Service
metadata:
  name: api-be
  annotations:
    cloud.google.com/backend-config: '{"default": "api-health-check"}'
spec:
  selector:
    app: api-be
  ports:
    - port: 80
      targetPort: {{ .Values.apiBe.port }}
